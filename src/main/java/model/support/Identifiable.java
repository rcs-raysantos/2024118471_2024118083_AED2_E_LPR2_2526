package model.support;

/**
 * Interface que defines que uma classe é identificável.
 * Implementando esta interface, uma classe possui um método getId().
 */
public interface Identifiable {
    String getId();
}
